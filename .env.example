# Copy to .env (gitignored) and fill in. Never commit real secrets.

# Anthropic API key (required for the LLM Planner).
ANTHROPIC_API_KEY=

# Optional overrides (defaults shown).
# FLIGHT_FINDER_PLANNER_MODEL=claude-haiku-4-5
# FLIGHT_FINDER_PLANNER_FALLBACK_MODEL=claude-sonnet-4-6
# FLIGHT_FINDER_CACHE_PATH=~/.flight_finder/cache.db
# FLIGHT_FINDER_LOG_LEVEL=INFO
